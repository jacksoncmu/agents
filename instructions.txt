Add a policy/rules engine using just-in-time structured injection.

Goal:
Move business rules out of fragile long-lived prompt memory and inject them right before relevant tool calls.

Requirements:
- Before specific tool calls, inject structured reminders/checks
- Examples:
  - before cancellation: eligibility checks
  - before delete: secondary confirmation
  - before compensation/refund: prerequisite checks
- These should be scoped to relevant actions, not dumped globally into the prompt

Implementation:
- policy/rules layer separate from engine and tools
- engine consults policy layer before executing relevant tools
- policy layer can:
  - allow
  - block
  - require_confirmation
  - inject structured reminder/guidance

Add tests for:
- correct interception
- correct block behavior
- correct confirmation gating
- correct injected reminder visibility
- preserved protocol ordering

Stop after this phase, summarize what you built, list files changed, list anything still broken, and wait for my next instruction.